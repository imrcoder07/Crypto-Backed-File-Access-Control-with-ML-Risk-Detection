services:
  - type: web
    name: crypto-backed-file-access-control
    env: python
    pythonVersion: 3.10.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
